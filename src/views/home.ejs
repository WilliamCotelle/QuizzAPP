<%- include("partials/header") %>

  <main class="container py-4">
    
    <!-- Welcome -->
    <div class="text-center">
      <h2 class="h1 my-4">Bienvenue sur O'Quiz</h2>
      <p class="lead">O’Quiz est votre plateforme idéale pour tester vos connaissances avec des quiz interactifs. Plongez dans des questions variées et apprenez de nouvelles choses à chaque quiz !</p>
    </div>

    <!-- Cards -->
    <div class="row mb-3 text-center flew-wrap">
        <!-- One card -->
        <% for (let quizz of quizzes){%> 
        <div class="col-4">
            <div class="card mb-4">
                <div class="card-header">
                    <h3 class="h2"><%= quizz.title %></h3>
                </div>
                <div class="card-body d-flex flex-column justify-content-between">
                    <h4 class="h3"><%= quizz.description %></h4>
                    <p><%= quizz.author.firstname %> <%= quizz.author.lastname %></p>
                    <ul class="list-unstyled mb-4 p-0">
                        <% for (let tag of quizz.tags){ %>
                       <li class="badge bg-primary"><%= tag.name %></li>
                       <% } %>
                    </ul>
                    <a href="/quiz/<%= quizz.id %>" class="btn btn-lg btn-primary text-white">Jouer</a>
                </div>
            </div>
        </div>
        <% }; %>
        
  </main>

<%- include("partials/footer") %>
